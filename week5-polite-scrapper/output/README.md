This folder holds the scraper's output. `books.json`, `errors.json`, and `run-report.json`
are generated fresh by every run (`python -m src.main`) and are not committed — see
`books.sample.json` for one verified real record showing the exact shape a full run produces,
and the README at the project root for a representative `run-report.json`.
